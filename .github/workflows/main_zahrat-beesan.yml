name: Build and deploy Node.js app to Azure Web App - zahrat-beesan

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies and build React app
        run: |
          npm install --legacy-peer-deps --no-audit --no-fund
          CI=false GENERATE_SOURCEMAP=false DISABLE_ESLINT_PLUGIN=true NODE_OPTIONS="--max-old-space-size=4096" npm run build
          
          # Create backward-compatible aliases for CSS and JS bundles
          MAIN_CSS=$(ls build/static/css/main.*.css | head -n 1)
          if [ -f "$MAIN_CSS" ]; then
            cp "$MAIN_CSS" build/static/css/main.ff9e555f.css
            cp "$MAIN_CSS" build/static/css/main.css
          fi
          
          MAIN_JS=$(ls build/static/js/main.*.js | head -n 1)
          if [ -f "$MAIN_JS" ]; then
            cp "$MAIN_JS" build/static/js/main.2d15e045.js
            cp "$MAIN_JS" build/static/js/main.b45cc53b.js
            cp "$MAIN_JS" build/static/js/main.js
          fi

      - name: Bundle server.js and prepare clean release ZIP
        run: |
          mkdir -p release
          npx --yes esbuild server.js --bundle --platform=node --target=node20 --outfile=release/server.js --external:canvas
          cp -r build release/
          cp package.json release/
          find release/build -type f \( -name "*.png" -o -name "*.mp4" -o -name "*.jpg" -o -name "*.jpeg" -o -name "*.webm" -o -name "*.mov" -o -name "*.gif" \) -delete
          cd release && zip -r -q ../release.zip * && cd ..
          echo "Release ZIP size: $(du -sh release.zip | cut -f1)"

      - name: Deploy to Azure Web App via Kudu API
        env:
          AZURE_WEBAPP_PUBLISH_PROFILE: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        run: |
          node scripts/azure-deploy.js